# DynMaskCO 当前状态

> 唯一状态入口 · 最后更新：2026-09-12 · 论文主线 v4（DEV-GATE GO，进入 CAL-PHYS（VAL 前置））

## 不可偏移的论文主线

论文目标固定为：

> **基于 MaskCO 的掩码生成范式，研究面向动态冷链物流的因果、约束可行、冷链效用对齐的在线优化方法。**

最终方法暂命名为 **DynMaskCO-CC**。它不是普通 dynamic CVRPTW 上的独立 CostAware 打分器，也不是把冷链字段作为附加特征；它必须同时保留并验证以下三层：

1. **MaskCO 方法内核**：对事件影响的可变车队—路线决策进行掩码、重构和迭代改进；
2. **动态在线机制**：只使用当前已揭示信息，冻结已执行前缀和 committed leg，并对动作做完整车队级约束认证；
3. **冷链优化目标**：显式维护温度、品质与制冷能耗轨迹，以终局冷链效用监督可部署动作。

Cost-aware preference learning 只作为 DynMaskCO-CC 的**效用对齐训练机制**，不再作为论文顶层研究目标。

## 主实验运营语义（已冻结）

v4 主实验固定为 **dynamic cold-chain pickup-to-depot**：每个动态订单是从客户取货并运回中心仓库的冷链请求，而不是仓库向客户配送。

- 车辆从仓库空载出发；订单在 `reveal_time` 后才可参与决策；
- 客户处货物在取货前由外部合规冷库保管，前置损耗视为固定；服务完成时货物进入车载对应温区，载重增加，策略相关品质计时从 `service_finish` 开始；
- 在途车辆可接收新揭示的取货请求，无需虚构 depot reload；
- 每车单次行程，返仓时一次性卸货并关闭，不允许 multi-trip/reload；
- 终局 complete 要求全部订单恰好取货一次、全部车辆返仓且 cargo manifest 为空。

这一语义与现有 `current_load += demand` 和返仓清零的状态转移一致。若以后改做 depot-to-customer 配送，必须新建协议版本并显式实现装货、货物 ownership 与返仓补货，不能静默复用当前结果。

## 当前科学结论

DynMaskCO 已完成 strict-online 协议修复、JF1-H 强基线、JF2 exact-vehicle 诊断和 HFR-M0 structural-imitation 验证。JF2 与 HFR-M0 均未建立优于 JF1-H 的下游在线效用，因此冻结，不再继续优化 physical-vehicle CE 或旧 adjacency CE。

这些负结果不否定 MaskCO 路线，而是界定下一版改进：**掩码生成的监督对象必须从静态路线结构，改为当前可执行动作的反事实终局冷链效用。**

## 当前执行位置

2026-09-03 已完成目录重排后的 P0-M 与 C0 功能闭环。P0-M 的路径、导入、代表入口和全脚本语法检查 7/7 通过；C0 的合同/状态/trace/snapshot/evaluator/teacher/旧口径隔离检查 20/20、pilot 参数方向敏感性 7/7 通过，P0-R/S/A/U 回归保持通过。C0 关闭的是实现与口径 Gate，不代表 pilot 参数已完成现实校准，也不产生方法性能结论。**O0-D 的 VAL128 Δ−2.668 / −10.9% 是旧 scope（含 committed-tail 插入）历史证据**，阶段 C scope 修复后不得再与新结果共用。hard-feasible baseline 定为 **JF1-H-F**（slack=1 + urgent-defer），Stage-A development Gate PASS、Stage-B scale v2 已冻结、阶段 C development Gate 已关闭。DEV-CAL 参与过 slack 调参，只属于 development data。旧 DEV-GATE 首 cell因计算版本漂移已归档为诊断；在 v4 归档、server preflight v2、归档外 regression 和严格 parity 全部通过后，**正式 DEV-GATE v4 已完成，verdict=GO**（run_id=`20260908-054550`，9×128，九 cell 等权 Δ=−0.2771，CI [−0.2848, −0.2696]，`results/o0cc/dev_gate_v4_official/`）。全程 0 error 实例、0 运行失败/补算。下一步：CAL-PHYS 现实校准 → 新 DEV 重确认路线 GO → 冻结 O0-CC VAL 协议后正式 VAL，VAL GO 才进入 M0。

| 阶段 | 状态 | 说明 |
|---|---|---|
| P0-R | ✅ 完成 | comparator 与 service-first 口径修复；B0/VAL32 已复核 |
| P0-S | ✅ 完成 | `RecourseSnapshotV2`、clone/resume 与 continuation parity |
| P0-A | ✅ 完成 | full-fleet Action Contract v1 与 feasibility certificate |
| P0-U | ✅ 完成 | distance-only common-continuation teacher 底座 |
| **P0-M** | **✅ 完成** | 集中路径解析、入口迁移、7/7 smoke/syntax 与 P0 全回归 |
| **C0** | **✅ 完成** | 功能/口径 20/20；pilot 参数方向 sensitivity 7/7；旧物理入口已退役或标记为非权威代理 |
| **O0-D** | **✅ 完成** | distance oracle headroom（**旧 scope 历史**：VAL128 Δ−2.668 / −10.9%，含 committed-tail） |
| **O0-CC** | **✅ DEV GO（VAL 未开始）** | DEV-GATE v4 九 cell 等权 Δ=−0.2771（CI 不跨 0）；CAL-PHYS → 新 DEV-CAL/profile → 新 DEV-GATE → O0-CC VAL；VAL GO 才进入 M0 |
| **阶段A** | **✅ 完成** | JF1-H-F 加固（3 阻塞点 + urgent-defer + P0 NEW_ROUTE scoped）；development Gate 9×128 全 0 |
| **阶段B** | **✅ 完成** | 重冻结 scale v2（`o0cc-pilot-devmean-equal-v2`：18.33/2.89/1193.49，1152/1152，v1 INVALIDATED） |
| 阶段C | **✅ 关闭** | oracle protocol repair 三轮 + 候选 repair 审计贯通；round-4/5 reg8 + 9 cell 跨场景回归全 PROTOCOL_PASS |
| M0 | ⏳ 未开始 | frozen MaskCO 表征探针；验证可学习性，不是最终论文方法 |
| M1 | ⏳ 未开始 | DynMaskCO-CC：效用对齐的事件掩码重构与在线补救 |
| A1/E1/T1 | ⏳ 未开始 | 归因、正式实验与一次性 TEST |

当前最近任务是 **现实校准 CAL-PHYS**，再在独立新 DEV 上重确认路线为 GO 后冻结正式 O0-CC VAL。正式 DEV-GATE v4 已完成（verdict=GO），旧首 cell 因版本漂移已归档为诊断，不重写原始漂移证据（见 `results/o0cc/dev_gate_drift_diag_v1/DIAGNOSTIC_NOT_OFFICIAL.md`）。DEV-GATE 分析清单已冻结（每 cell 128 共 1152，九 cell 等权 sequential 平均配对 Δ 为主终点，按内部 seed 成组 bootstrap）。在 M0/M1 和正式校准完成前，不把当前 pilot quality/energy 数字写成论文结论；正式 VAL 在 CAL-PHYS、新 DEV 复核与 VAL seed/manifest 冻结完成前不生成、不冻结、不查看。

**对比基线（`CC_Compare/`，独立于 DEV-GATE）**：**OR-Tools-RH-D 已正式冻结**（protocol revision 1 + identity manifest revision 1）。OR6 行为 Gate PASS、OR6.1/OR6.2 证据控制链闭合；OR7 预算扫描（`solution_limit={10,30,100}`）三档全通过协议 Gate（OR7-A 六批 parity 18/18、OR7-B 三档 288/288 complete 0 fallback），预注册选择器选定 **solution_limit=30**（九 cell 等权 pure distance 17.3956）。冻结三件套 `SOURCE_MANIFEST.json` ← `FROZEN_CONFIG.json` ← `FREEZE_SEAL.json`（核验 `verify_freeze.py` + `verify_or7_freeze.py`）。旧 `devproto_9x2_v3` 及更早批次为 control_hash 绑定前的历史证据，不通过当前复验。

## 已冻结证据

| 方法/结果 | `distance_cost` | complete | 使用方式 |
|---|---:|---:|---|
| OR-Tools-RH | 23.04 | 100% | strict-online 强基线 |
| JF1-H | 24.50 | 100% | 当前研发基线 |
| JF2 canonical | 25.91 | 100% | exact-vehicle 负结果 |
| HFR-M0 g_only | 24.97 | 100% | 结构信号存在，但未超过 JF1-H |
| HFR-M0 a_only | 26.63 | 100% | 旧路线结构监督无部署效用 |
| HFR-M0 full | 29.95 | 99.2% | service Gate 失败 |

表中 historical `complete` 是 distance 协议下的客户访问与返仓完整性；它尚未包含 v4 的 cargo manifest / `delivered_to_depot` 检查，不能直接当作 C0 后的冷链 complete。

已复核机制证据：`G_fleet=17.891%`、`G_seq=-1.227%`，VAL32 partition×sequencing interaction 为 `-1.7430`，95% bootstrap CI `[-2.387,-1.104]`。这些结果支持学习**联合车队—路线动作**，但不等于 CostAware 或 DynMaskCO-CC 已经有效。

## 论文成立的最小证据链

```text
P0-M 新目录导入与回归闭环
→ C0 冷链 evaluator 与单位闭环
→ O0-D / O0-CC 证明动作空间存在距离与冷链 headroom
→ M0 证明 MaskCO 表征能够学习动作效用
→ M1 证明“效用对齐的事件掩码重构”改善完整在线轨迹
→ MaskCO 身份归因与冷链机制归因同时成立
→ 多分布、EDoD、温度压力、规模和 5 seeds 稳健
→ VAL 冻结后 one-shot TEST
```

任一完整方法必须先达到 100% service 与硬约束可行；随后分别报告纯距离 `J_dist` 与冷链目标 `J_CC`，二者不得混名或互相替代。

## 权威文档顺序

1. [`docs/当前规划/README.md`](docs/当前规划/README.md)
2. [`docs/当前规划/研究设计/科研方法创新主控文档.md`](docs/当前规划/研究设计/科研方法创新主控文档.md)
3. [`docs/当前规划/工程实现/代码实现蓝图.md`](docs/当前规划/工程实现/代码实现蓝图.md)
4. [`docs/当前规划/结果与进度/执行进度表.md`](docs/当前规划/结果与进度/执行进度表.md)
5. [`docs/当前规划/实验与评估/实验执行手册.md`](docs/当前规划/实验与评估/实验执行手册.md)
6. [`docs/当前规划/实验与评估/评估口径.md`](docs/当前规划/实验与评估/评估口径.md)
7. [`docs/当前规划/决策与审计/风险与解决预案.md`](docs/当前规划/决策与审计/风险与解决预案.md)

## 历史材料使用规则

- `14.77`、`15.45`、`14.08` 以及旧“100% feasible”来自 P0 审计前协议，不得作为当前主结果。
- `品质机制调试总结.md` 中“减少 96% 损耗”及 weighted-completion-time 排序来自旧配送式口径；与 v4 pickup 暴露时间定义不兼容，不是权威论文证据。
- JF2/HFR 资料用于解释为什么监督目标转向冷链终局效用，不决定当前实现方向。
- 任何 cost 排名必须先满足 complete/service-first blocking Gate。

## 不可破坏约束

- 不修改 `../MASKCO_code/` 中的原始 MaskCO 源码；全部新实现和路径适配位于本工作区。
- 不修改冻结的 `data/baseline/` 文件内容。
- 不在 CVRPTW 子目录添加 `__init__.py`。
- 保持 strict-online、visible-only normalization、frozen-prefix、committed invariance 和 exact-once。
- `distance_cost` 永远表示纯行驶距离；冷链复合目标必须使用独立字段 `coldchain_cost`。
- C0 唯一权威路径由 `coldchain-contract-v1`、`coldchain_state.py` 和 trace evaluator 承担；旧 `thermal_state.py` 已改为统一回放兼容桥并拒绝歧义单段调用，预计算 `quality_loss/energy_mat` 与旧 `Num unsalable` 统一标记为 `legacy` 非权威代理，不得与 C0 指标混用。
