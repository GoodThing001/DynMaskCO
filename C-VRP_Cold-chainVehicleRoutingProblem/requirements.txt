# DynMaskCO — C-VRP_Cold-chainVehicleRoutingProblem Python dependencies
#
# The parent MaskCO environment (JAX / Flax / Triton / PyTorch / NumPy) is installed
# via `sh install.sh` in the repository root. The versions below are the sub-directory
# specific additions for the cold-chain / dynamic extension and its baselines.

# --- Core numerical (pinned to the frozen evaluation environment) ---
numpy==1.26.4

# --- C++ extension build ---
pybind11>=2.11

# --- Baselines (OR-Tools clairvoyant + rolling-horizon) ---
ortools>=9.8
