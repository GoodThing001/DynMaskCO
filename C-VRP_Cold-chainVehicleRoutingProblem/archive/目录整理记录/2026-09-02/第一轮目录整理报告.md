# 第一轮目录整理报告

> 执行日期：2026-09-02

## 范围

本轮采用低风险整理：统一状态和索引、归拢根目录外围文件、清理可再生缓存。没有重排算法源码、数据集或实验结果目录。

## 新增入口

- 根工作区导航：`工作区导航.md`
- DynMaskCO 唯一状态入口：`项目当前状态.md`
- 文档导航：`docs/README.md`
- 结果状态索引：`results/README.md`
- 数据现状清单：`data/数据目录现状清单_2026-09-02.md`

## 文档同步

- 将当前状态更新为：P0-R/P0-S/P0-A/P0-U 已完成，下一步 O0。
- README 不再把 `14.77`、`15.45`、`14.08` 作为当前结果。
- HFR 长设计文档增加冻结/历史状态说明。
- `AGENTS.md` 和两个 `CLAUDE.md` 改为引用唯一状态入口。
- `results/README.md` 将结果区分为 CURRENT、VALID BASELINE、EVIDENCE、FROZEN NEGATIVE、SUPERSEDED、SMOKE 和 INVALIDATED。
- 数据文档明确：冻结 manifest 覆盖 30 个核心文件；后加 mixed 文件不在旧 manifest 内；两个同名 mixed 文件 SHA256 不同。

## 文件归拢

- 两个根目录下载压缩包移动到 `workspace_assets/downloads/`，未删除。
- Cursor 清理工具移动到 `workspace_assets/tools/`。
- `plot_experiment_trends.py` 保留原位，因为其输出目录依赖当前位置。

## 清理

- 删除 21 个 `__pycache__` 目录，共 108 个可再生字节码文件。
- 删除两个确认不含文件的临时解压根目录。
- 未删除空日志目录、checkpoint 占位目录、数据、raw results 或下载压缩包。

## 恢复与审计

- `整理前文件清单_第一轮.csv`：第一轮整理前路径、大小和时间清单。
- `整理后文件清单_第一轮.csv`：第一轮整理后清单。
- `关键文件校验_第一轮整理前.csv`、`关键文件校验_第一轮整理后.csv`：关键说明文件 SHA256。
- `路径迁移表.md`：路径迁移和删除记录。

审计结果：父项目受保护的 `.py/.cpp/.hpp/.h/.c/.cc/.cu/.sh` 文件变化数为 **0**；整理后残留 `__pycache__` 数为 **0**。

## 验证

- P0-R comparator protocol：18/18 PASS。
- P0-S RecourseSnapshotV2：7/7 PASS。
- P0-A Action Contract：7/7 PASS。
- P0-U counterfactual teacher：4/4 PASS。
- 新增/修改入口文档的本地链接已检查。

## 暂未执行

- 不初始化 Git。
- 不删除 1.12 GB checkpoint 下载包。
- 不物理迁移 `results/` 历史目录。
- 不物理迁移被大量文档引用的 v1/v2/v3 文件。
- 不合并两个同名但内容不同的 mixed 数据文件。

这些项目留给第二轮深度整理，执行前应再次确认。
