# DynMaskCO-CC 执行进度表

> 版本：2026-09-08 v7
> 图例：✅完成 · 🔄当前 · ⏳未开始 · ⛔冻结/No-Go。  
> 本文件是唯一实时进度入口；科学边界见主控文档，代码接口见代码实现蓝图。

## 0. 当前一句话状态

> JF2/HFR 已作为负结果冻结，P0-R/S/A/U/P0-M/C0 全部完成。**O0-D 的 VAL128 Δ−2.668 / −10.9% 是旧 scope（含 committed-tail 插入）历史证据**，阶段 C scope 修复后不得再与新结果共用。hard-feasible baseline 定为 **JF1-H-F**（slack=1 + urgent-defer），Stage-A development Gate、Stage-B scale v2 与阶段 C protocol Gate 均已通过。旧 DEV-GATE 首 cell 因版本漂移归档为诊断；v4 归档、server preflight、独立 regression 与严格 parity 已全部 PASS。**正式 DEV-GATE v4 已完成，verdict=GO**（run_id=`20260908-054550`，9 cell × 128，输出 `results/o0cc/dev_gate_v4_official/`，九 cell 等权 Δ=−0.2771，CI [−0.2848, −0.2696]）。下一步：CAL-PHYS 现实校准 → 新 DEV-CAL/profile → 新 DEV-GATE 重确认 → 冻结 O0-CC VAL 协议后正式 VAL。

## 1. 总阶段看板

| 阶段 | 内容 | 状态 | 通过条件 |
|---|---|---|---|
| P0-R | comparator/service-first | ✅ | 18/18；B0/VAL32 重跑完成 |
| P0-S | RecourseSnapshotV2 | ✅ | 7/7；301 snapshots exact parity |
| P0-A | full-fleet Action Contract v1 | ✅ | 7/7；ownership/certificate PASS |
| P0-U | distance counterfactual teacher | ✅ | 4/4；incumbent delta=0 |
| **P0-M** | `MASKCO_code/` 目录迁移兼容 | **✅ 完成** | 7/7 路径/导入/入口/syntax；P0-R/S/A/U 回归 PASS |
| **C0** | pickup manifest、冷链合同、状态、trace evaluator | **✅ 完成** | 20/20 功能/口径 + 7/7 pilot sensitivity |
| **O0-D** | distance sequential oracle | **✅ 旧 scope sanity** | 历史结果只作协议线索，不与阶段 C 新 scope 共用 |
| O0-CC | cold-chain sequential oracle | **✅ DEV-GATE GO（VAL 未开始）** | CAL-PHYS → 新 DEV 重确认 → 冻结 VAL → 正式 VAL |
| CAL-PHYS | 现实物理/品质校准与 contract vNext 前置 | **🔄 预注册草案（未冻结）** | 源码字节基线已 sealed，待提交 E；现实阻断字段清空；协议/分析/运行表 freeze seal 完整 |
| M0 | frozen MaskCO utility probe | ⏳ | Real/MaskCO 信号优于 shuffle/generic |
| M1 | DynMaskCO-CC masked recourse | ⏳ | 100% service + \(J_{CC}\) 主 Gate |
| M2 | on-policy aggregation | ⏳ | 按需，最多 1–2 轮 |
| A1 | MaskCO/冷链/动作归因 | ⏳ | 三类核心贡献均有增量 |
| E1 | 论文级矩阵 | ⏳ | dynamics + thermal stress + 5 seeds |
| T1 | one-shot TEST 与论文 | ⏳ | 全部冻结后一次运行 |

## 2. 已冻结历史结论

| 证据 | 结果 | 状态/用途 |
|---|---:|---|
| JF1-H VAL128 | 24.5019 / 100% | distance 研发基线 |
| OR-Tools-RH | 23.04 / 100% | strict-online 强参考 |
| JF2 canonical | 25.91 / 100% | exact vehicle label 负结果 |
| HFR g_only | 24.97 / 100% | 结构信号存在，未超 JF1-H |
| HFR a_only | 26.63 / 100% | 旧 route target 无部署效用 |
| HFR full | 29.95 / 99.2% | service Gate FAIL |
| B0 \(G_{fleet}\) | 17.891% | P0-R 后已复核 |
| B0 \(G_{seq}\) | -1.227% | sequencing 已接近饱和 |
| VAL32 interaction | -1.7430，CI [-2.387,-1.104] | 支持 coupled action 动机 |

以上都是 distance 协议证据；不证明现有 quality/energy 或 DynMaskCO-CC 已有效。

> **对比基线（`CC_Compare/`，独立于主实验）**：PyVRP-RH-D 与 **OR-Tools-RH-D 均已正式冻结**（OR-Tools-RH-D：protocol revision 1 + identity manifest revision 1，OR7 预算扫描选定 `solution_limit=30`，九 cell 等权 pure distance 17.3956）。主表候选待继续适配 RRNCO-RH / CaDA-RH。

## 3. 已完成 P0

### P0-R ✅

- [x] partial route 不再返回 success；
- [x] \(k>8\) 显式 unavailable，不 drop；
- [x] returned set == assigned set；
- [x] service-first 逐实例矩阵；
- [x] B0 train256 与 VAL32 factorial 重跑；
- [x] raw status、coverage 与 paired CI 保存。

主要产物：`results/p0r/`。

### P0-S ✅

- [x] 有序 mutable suffix、served trace、committed state；
- [x] capture/clone/restore/resume；
- [x] initial/reveal/exhaustion/WAIT/committed/returning；
- [x] asymmetric distance；
- [x] 301 snapshots exact continuation parity。

### P0-A ✅

- [x] anchored slot + anonymous NEW_ROUTE；
- [x] stable action ID；
- [x] 所有 insertion positions；
- [x] full-fleet apply；
- [x] ownership exactly once；
- [x] TW/cap/return certificate；
- [x] permutation 与 brute-force coverage tests。

### P0-U ✅

- [x] common JF1-H continuation；
- [x] distance-only terminal lexicographic outcome；
- [x] force plan hash；
- [x] same action deterministic；
- [x] incumbent delta=0；
- [x] listwise candidate table。

P0-U 的默认 distance 路径保持不变；C0 已新增显式 `objective="coldchain"` 路径。

## 4. P0-M：目录迁移兼容 ✅

- [x] 新建 `scripts/project_paths.py`，唯一解析工作区、`MASKCO_code/`、扩展和 scripts 根目录；
- [x] 修正静态扫描发现的旧路径入口及后续补扫文件；
- [x] 训练/解码/仿真/评估代表入口 `--help` 或 import smoke；本机训练入口因缺 `tensorboardX`，仅在路径 smoke 中使用显式内存 stub，未冒充完整训练环境；
- [x] 从工作区根目录、扩展根目录和任意临时 cwd 得到相同路径；
- [x] 确认没有修改或复制 `MASKCO_code/` 源码；
- [x] P0-R 18/18、P0-S 7/7、P0-A 7/7、P0-U 4/4 回归仍 PASS；
- [x] 保存 `results/p0m/project_layout_tests.json`、迁移文件清单与环境记录。

Gate P0-M：全部通过后才进入 C0。该 Gate 只恢复工程可执行性，不新增论文性能结论。

## 5. C0：冷链闭环 ✅

### C0-0 合同冻结

- [x] 主实验锁定为 dynamic cold-chain pickup-to-depot；
- [x] 车辆锁定为同构多温舱、共享总容量、单次行程、无 reload；
- [x] 实现距离、时间、功率、能量和温度单位合同；
- [x] 实现品质函数、阈值、参数来源标签与 sensitivity 范围；当前数值明确标为 pilot，尚待文献/数据校准；
- [x] 实现 \(J_{CC}\) scales/weights 和 hard/soft 边界；当前权重为 pilot；
- [x] 建立 `coldchain-contract-v1` 与 SHA-256 contract hash。

### C0-1 唯一状态转移

- [x] 新建 `scripts/coldchain/coldchain_contract.py`；
- [x] 新建 `scripts/coldchain/coldchain_state.py`；
- [x] 实现订单级 cargo manifest 与总/分温区载重守恒；
- [x] `service_finish` 入舱，`return_arrival` 卸货并关闭车辆；
- [x] 行驶、等待、服务/开门、返仓全部累计；
- [x] 统一 J→kWh 与 normalized temp_class；旧独立热状态已退役，模型代理修正反归一化；
- [x] 品质按实际舱温进入 Arrhenius 速率；
- [x] 状态转移为纯函数，不原地污染输入。

### C0-2 strict-online 接线

- [x] 扩展 VehicleState/ServiceRecord/VehicleTrace；
- [x] 区分 `picked` 与 `delivered_to_depot`；
- [x] 建立 `recourse_snapshot_v3_cc`，并保留 distance-only v2 reader；
- [x] cold-chain state resume exact parity；
- [x] future cold-chain feature perturbation PASS。

### C0-3 evaluator

- [x] 新建 `scripts/evaluation/coldchain_evaluator.py`；
- [x] `authoritative_evaluator` 在显式 cold-chain contract 下消费真实 trace，不再固定返回零 quality/energy；
- [x] `thermal_state` 仅桥接统一回放并拒绝歧义单段调用；cvrptw/beam/rolling-horizon/data 旧量均显式标为非权威 `legacy` 代理；
- [x] 原始分量可重算 \(J_{CC}\)，逐段累计与终态一致；
- [x] distance-only 输出通过 P0-R/S/A/U exact regression。

### C0-4 tests

- [x] `test_coldchain_contract.py` 20/20（覆盖蓝图要求、冷链教师、静态 pickup 回放、旧热入口退役、桥接 parity 与代理隔离）；
- [x] 输出 `results/c0/coldchain_contract_tests.json`；
- [x] `test_coldchain_sensitivity.py` 7/7，小规模方向敏感性产物已保存；
- [x] 所有 P0-R/S/A/U 回归仍 PASS。

Gate C0：✅ 通过。当前参数仍明确为 pilot；现实校准和论文级 robustness 仍是后续正式实验前置条件，7/7 仅证明实现方向响应可解释。

## 6. O0-D

- [x] 新建 `scripts/simulation/sequential_oracle.py`（greedy sequential oracle；customer order 冻结为 `tw_end → reveal_time → customer_id`；逐个 greedy，不做笛卡尔积联合搜索）；
- [x] 新建 `scripts/evaluation/run_action_oracle.py`（baseline vs sequential oracle + paired bootstrap CI + service-first Gate）；
- [x] synthetic 2–5 customer（`test_sequential_oracle.py` 3/3：no-regression 8.0==8.0 / headroom 30→22 Δ−8 / on-trajectory trace 分叉）；
- [x] service/hard feasibility 100%（synthetic 已验）；
- [x] VAL16 smoke（服务器 16/16：baseline 24.272 → oracle 21.795，Δ−2.478（−10.2%），95% CI [−2.849,−2.137] 不跨 0，service 100%）；
- [x] VAL32（服务器 32/32：baseline 24.413 → oracle 21.811，Δ−2.603（−10.7%），95% CI [−2.862,−2.355] 不跨 0，service 100%；与 VAL16 高度一致）；
- [x] VAL128 sequential on-trajectory（服务器 128/128：baseline 24.502 → oracle 21.834，Δ−2.668（−10.9%），95% CI [−2.829,−2.508] 不跨 0，service 100%；O0-D 正式通过）；
- [x] paired distance CI（VAL16/VAL32/VAL128 均 CI 不跨 0）；
- [ ] candidate/fallback/action distribution/runtime（可选诊断，headroom 结论已由 VAL128 冻结）。

O0-D 是协议 sanity。No-Go 时先扩 action，不训练 M0。sequential oracle 逐客户×逐 action 做完整 JF1-H rollout，50-node VAL 成本高，需在服务器跑。

## 7. O0-CC

### 7.0 协议修复（2026-09-04，阻塞项 1–11 全部完成）

- [x] 具名 objective profile（`ObjectiveProfile` + `apply_objective_profile`，scale provenance 放宽）；
- [x] headroom 三态 Gate（GO / EVIDENCE_INSUFFICIENT / NO_HEADROOM，按 CI 上界判定）；
- [x] `service_ok` 完整冷链硬 Gate（温度硬约束 / 入舱 / 返仓 / manifest 清空 / trace accounting）；
- [x] 显式 KEEP/DEFER（JF1-H 遗漏客户时评估「保持遗漏」vs「插入」，避免虚假硬指标改善）；
- [x] oracle 动作时机冻结（只在 `replan_ids` 非空时决策，与 JF1-H 决策频率一致）；
- [x] local baseline-state oracle（`run_local`）+ sequential 双报告 + 逐事件日志 `oracle_log.jsonl`；
- [x] 统一路径解析迁移（project_paths）+ diagnose ratio 语义修正；
- [x] 测试补齐（多客户同事件 / 动作时机 / 冷链 Gate / coverage）；全量回归 P0-R/S/A/U/C0/P0-M/O0 全 PASS。

### 7.1 scale 冻结（DEV-CAL，决策 1）

- [x] 新建独立 `data/baseline/50_node/dev_cal/`（9 cell = R1/C1/RC1 × EDoD 0.2/0.5/0.8，每 cell 128 实例，seed=777，含 DEV_CAL_MANIFEST.json 的生成参数 + 代码/文件 sha256）；
- [x] 冻结 **`o0cc-pilot-devmean-equal-v1`**：`distance_scale=19.4706 / quality_scale=2.8126 / energy_scale=1244.016`（9 cell 等权算术均值），`λ_Q=λ_E=1.0`（等权）；profile_hash `d58c399b8cfcbaaa…`；
- [x] 归一化后 baseline 三分量贡献 = (1.0, 1.0, 1.0)（等权定义，诊断口径，非经济权重）。

### 7.2 旧 scale 结论（已作废）

- ⚠️ 旧 VAL8/VAL16 结果用的是 pilot scale（scale 全 1.0，λ_E=0.1），energy 主导 83.9%、quality 淹没 1.8%——**该结论只能标「结论无效/待重跑」，不能称为真实 No-Go**（导师纠偏：scale 修正后必然转正不成立，重算 +0.074 抵消项变 quality）。

### 7.3 O0-CC 重跑（新 scale o0cc-pilot-devmean-equal-v1）

- [x] DEV-CAL 8 实例 smoke（`results/o0cc/devcal8_seq`）：baseline 3.232 → oracle 3.193，**Δ−0.039，CI [−0.178,+0.138] 跨 0，verdict=EVIDENCE_INSUFFICIENT**；逐实例 **6 改善 / 2 恶化**（实例 5 大恶化 +0.47 拉平）；
- [x] 分项：distance −1.601（CI [−2.612,−0.541] 显著改善）／ quality +0.084（CI 跨 0）／ energy +16.9（CI 跨 0）／ unsalable/thermal 均 0；
- [x] **方向翻转确认**：旧 pilot scale 的「NO_HEADROOM」是 energy 尺度失配伪影；等权归一化后 distance 改善主导，复合目标方向转负（但仍证据不足）；
- [x] **实例级多进程 + parity**：`run_action_oracle.py` 加 `--workers`（每 worker 独立跑单实例 baseline+sequential+local，BLAS 线程数=1，任一失败整体不静默，逐实例落盘）；本地 Windows spawn + 服务器 Linux fork 均验证 `workers=1 vs 2` 的 per_instance（忽略 runtime）与 oracle_log **exact identical**；
- [x] DEV-CAL32 预演（`results/o0cc/devcal32`，coldchain + local + sequential，workers=8，32 实例）：sequential Δ−0.035（CI 跨 0）、local Δ−0.022（pooled 3228 决策点，CI 过窄）——**但 baseline 31/32 失败（JF1-H 漏 1 订单 `n_unserved=1`，TW/cap/return/temperature/accounting 均 feasible）→ verdict 应为 SERVICE_GATE_FAIL，本轮 headroom 结论不成立**；
- [x] 修正 verdict 优先级：baseline/oracle 非 100% hard-feasible → `SERVICE_GATE_FAIL`（在 GO/EVIDENCE_INSUFFICIENT/NO_HEADROOM 之前）；
- [x] 确定 hard-feasible baseline = **JF1-H-F**（JF1-H + 完整计划层 repair + 显式延期 + urgent-defer），并通过阶段 A development Gate；
- [x] **阶段 A 三阻塞点加固**（导师复核指出的削弱 PASS 可信度的问题）：
  - 阻塞点 1 写回范围：repair 枚举只限定 `mutable_ids`（replan 的 idle/ready），不再把客户插入 committed/非 replan 车却不写回；
  - 阻塞点 2 repair_status：改 `clean/repaired/partially_deferred/deferred/ownership_error`（旧 `repair_failed` 是死分支）；
  - 阻塞点 3 ownership：改 Counter 全 partition 校验（committed/suffix/deferred 交叉 + served/future/extra/duplicate/missing）；
- [x] 修 deferred 语义 bug（deferred 客户被重新分配/commit 后未从集合移除 → `committed_and_deferred` / `suffix_and_deferred` 交叉，`ownership_violations=14033` → 0）；
- [x] 修 greedy composition 硬 fail：**urgent-defer 规则** —— 被丢客户若能被 committed 车在 tw_end 前服务则延期（`deferred_committed_reachable`），reserved slack（NEW_ROUTE）只给真正无法等待的客户；否则 slack 会被第一个 drop 客户消耗，late-reveal tight-window 客户仍 unserved（`hard_fail=3` → 0，无回归）；
- [x] **P0 复核补丁（第二轮导师复核）**：
  - NEW_ROUTE scoped physical matching：`apply_action` + `_match_new_route` 增 `allowed_vehicle_ids`，枚举 candidate plan hash 与最终 apply 用同一过滤范围，消除「枚举按 allowed 车算、apply 落到别的空 idle 车」的 scope 越界（phantom insertion 同类）；
  - ownership committed 改 Counter，检测 `duplicate_committed`（两车同 committed_next 的交叉）；
  - urgent-defer 补容量 + 返仓检查（`_can_defer_to_committed` 三条件：时间/容量/返仓 horizon）；
  - deferred 记账拆分 `deferred_then_reassigned / deferred_then_served`（terminal_deferred = terminal_unresolved）；
- [x] **Stage-A development Gate PASS**（`audit_baseline_service.py`，DEV-CAL 9×128，`--slack 1`，显式 hard vector + 逐实例 JSON + repair_events.jsonl + manifest，强制 9 cell + 128 实例校验 + 源码 hash 链）：`hard_fail=0 / terminal_unresolved=0 / ownership_violations=0`；
- [x] 对账：cells_n_sum=1152、instances=1152、repair_events scene ids ⊆ instances、9 唯一 cell、deterministic parity（同批 8 实例跑两次完全一致）；
- [x] 回归测试 `test_jf1h_repair.py`（writeback 一致性含非 mutable 车不变 / greedy report==base / hard Gate）+ `test_action_contract.py`（+`new_route_scoped_matching`）8/8；`test_sequential_oracle.py` 6/6 回归 PASS；
- [x] 阶段 B：JF1-H-F 重冻结 scale v2 → 修 oracle protocol（阶段 C）→ DEV-GATE（旧首 cell 版本漂移归档为诊断，见 §当前一句话状态）；
- [x] **阶段 B 完成**：`freeze_coldchain_scale.py` 改用 `JF1HRepairReplanner(slack_vehicles=1)`，校验 DEV-CAL manifest + 9 cell + 128 实例 + 数据 hash，逐实例硬 Gate（任一失败退出），临时文件 reload 复算 profile hash 一致后发布。**`o0cc-pilot-devmean-equal-v2`**：`distance_scale=18.3323 / quality_scale=2.8917 / energy_scale=1193.4934`，`λ=(1,1)`，profile_hash `df1f7a6e…`，hard Gate 1152/1152，Stage-A 与 freeze 的 7 个共同代码 hash + contract hash 一致；v1 标 INVALIDATED（reason: non-hard-feasible original JF1-H baseline）；
- [x] 修 local oracle 定义（单客户 single-action，不调完整 sequential_oracle_plan；阶段 C 已完成）；
- [x] 修 local 统计单位（customer→event→instance，instance/seed-group bootstrap；阶段 C 已完成）；
- [x] 修 local eligibility（只在 replan_ids 非空时采样；阶段 C 已完成）；
- [x] 补测试/日志（DEFER parity、lexicographic min、cluster CI、coverage、divergence）——已由阶段 C round-4/5 完成；
- [x] 修正后重跑非重叠 DEV-CAL（回归 + DEV-CAL64/128 确认）——已由阶段 C 跨场景 9/9 回归取代；lookahead 未进入。

No-Go 顺序（只在真实 No-Go 后触发）：local 有/sequential 无 → lookahead（1–2 步）→ two-customer relocate/exchange → batch mask action。

## 8. M0：MaskCO 表征探针

- [ ] 冻结 pretrained causal MaskCO encoder；
- [ ] cold-chain utility head；
- [ ] state-normalized pair/list loss；
- [ ] offline/on-policy normalized regret；
- [ ] pretrained vs random/generic/feature-only；
- [ ] Real vs Shuffle；
- [ ] strict-online final \(J_{CC}\)。

M0 Go 只表示“可学习”；不能直接作为最终论文方法。

## 9. M1：DynMaskCO-CC

- [ ] fleet/thermal tokens；
- [ ] event/risk affected mask；
- [ ] certified action tokens；
- [ ] utility-aligned masked decoder；
- [ ] 1–K 轮 iterative reconstruction；
- [ ] full-fleet apply、guard 与 complete incumbent fallback；
- [ ] adapter-first 训练；
- [ ] future invariance；
- [ ] VAL128 100% service；
- [ ] \(J_{CC}\) 优于 JF1-H-CC；
- [ ] distance 退化在预注册预算内；
- [ ] event mask 与 iterative reconstruction 均有实质增量。

## 10. M2/A1

M2（仅需要时）：

- [ ] first-divergence、高 regret、fallback、高热风险 states；
- [ ] 相同合同重新标注；
- [ ] 最多两轮；
- [ ] offline vs aggregated。

A1：

- [ ] pretrained MaskCO vs random/generic/feature-only；
- [ ] event mask vs no/random/shuffle；
- [ ] iterative vs one-shot；
- [ ] true vs removed/shuffled thermal state；
- [ ] distance/quality/energy/joint objective；
- [ ] joint vs slot-only/insertion-only；
- [ ] Real vs Shuffle labels。

## 11. E1/T1

E1：

- [ ] R1/C1/RC1 × EDoD 0.2/0.5/0.8；
- [ ] balanced/chilled-heavy/frozen-heavy；
- [ ] normal/high thermal stress；
- [ ] 50-node 5 seeds；
- [ ] 100-node scale；
- [ ] JF1-H、JF1-H-CC、OR-RH、OR-RH-CC、外部 learned baseline；
- [ ] paired statistics、Pareto/trade-off、runtime/memory。

T1：

- [ ] 模型、合同、权重、统计计划冻结；
- [ ] TEST ledger；
- [ ] one-shot TEST；
- [ ] raw artifacts；
- [ ] 论文主张边界核对。

## 12. 当前下一步

```text
DONE-P0M    统一路径解析、入口迁移、7/7 smoke 与 P0 全回归
DONE-C0-1   coldchain-contract-v1 + 唯一纯状态转移
DONE-C0-2   strict-online trace + snapshot v3 + evaluator
DONE-C0-3   action certificate/teacher 冷链扩展
DONE-C0-4   旧物理入口退役/桥接；历史代理字段显式隔离
DONE-C0-5   功能/口径 20/20 + pilot sensitivity 7/7，关闭 C0 Gate
DONE-O0-D   distance oracle（VAL128 Δ−2.668 / −10.9%，CI 不跨 0；旧 scope 历史证据）
DONE-阶段A  JF1-H-F hard-feasible baseline 加固（3 阻塞点 + urgent-defer + P0 NEW_ROUTE scoped；dev Gate 9×128 全 0）
DONE-阶段B  JF1-H-F 重冻结 scale v2（o0cc-pilot-devmean-equal-v2，1152/1152，reload hash exact，v1 INVALIDATED）
DONE-阶段C  oracle protocol repair（三轮 + 候选 repair 审计 + 跨场景 9/9 + 接线回归 + 聚合入口加固）
DRIFT-DIAG  旧 DEV-GATE 首 cell 因计算版本漂移归档为诊断（五实例 parity ALL_MATCH，非正式判定依据）
PASS-PREFLIGHT  v4 归档 + server preflight v2 + 独立 regression + strict parity
DONE-O0-CC-DEV  正式 DEV-GATE v4 GO（run_id=20260908-054550；9×128；九 cell 等权 Δ=−0.2771，CI 不跨 0；正式 O0-CC 待 VAL 后关闭）
NEXT-CAL-PHYS 现实物理校准（CAL-PHYS）；源码基线已 sealed，文档包仍为 draft_blocked；提交 E 后回填现实字段，先注册并执行 P1，再冻结 P2–P6
NEXT-VAL      冻结 O0-CC VAL seed/manifest/统计方案后一次性正式 VAL（CAL-PHYS 与新 DEV 复核完成前不生成/不冻结/不查看）
```

pilot DEV-GATE v4 headroom 已判定为 GO（九 cell 等权 Δ=−0.2771，CI 不跨 0），O0-CC 正式结论待 calibrated VAL；旧批次仅作诊断、不重写原始漂移证据（见 `results/o0cc/dev_gate_drift_diag_v1/DIAGNOSTIC_NOT_OFFICIAL.md`）。在 M0/M1 和正式校准完成前，不把当前 pilot quality/energy 数字写成论文结论。

### 正式 DEV-GATE v4（已完成，verdict=GO）

- run_id：`20260908-054550`；输出：`results/o0cc/dev_gate_v4_official/`（含 `dev_gate_summary.json` + `EVIDENCE_SEAL.json`）；
- 数据：DEV-GATE seed=7782，精确 9 cell × 128，instance set=`0..127`，九 cell 共用内部 seed 序列；
- 冻结身份：bundle `1bbcc978…` / compute `2dfff7ae…`（15 文件）/ runner `b74ea7cb…` / control `49019c4a…` / analysis `cd266de1…` / profile `df1f7a6e…`；
- 配置：capacity=50、vehicles=25、slack=1、objective=coldchain、local=true、baseline=JF1-H-F、bootstrap=10000/42、workers=8；
- 结果：verdict=GO，九 cell 等权 Δ=−0.2771，成组 bootstrap 95% CI [−0.2848, −0.2696]；分项 Δdistance=−0.2912 / Δquality=−0.5645 / Δenergy=−78.83；local 九 cell 等权 Δ=−0.0196（CI 不跨 0）；
- 运行账本：attempts 9 START + 9 SUCCESS + 0 FAIL（无重试/补算），0 error 实例，0 归档 pyc；
- 下一步：CAL-PHYS 现实校准 → 新 DEV-CAL/profile → 新 DEV-GATE 重确认 sequential GO → 冻结 O0-CC VAL 协议后正式 VAL；VAL GO 后才进入 M0。

### CAL-PHYS 预注册文档包（2026-09-13，DRAFT-BLOCKED）

- 科学范围：`docs/当前规划/实验与评估/CAL-PHYS/CAL_PHYS_SCOPE_DECISION.md`，推荐 `coldchain-contract-v2-light`；
- 机读协议：`CAL_PHYS_PROTOCOL.json`；实验单位为独立车辆/舱体运行与独立商品批次，禁止把传感器、时间点或同批次样品当作独立 n；
- 设计：blocked split-plot factorial，按 vehicle/day/operator/sensor/lot 阻断，在可执行 block 内固定 seed 随机顺序；holdout 必须是完整未见运行/完整未见商品批次；
- 分析：`CAL_PHYS_ANALYSIS_PLAN.json`；nominal 唯一 gating，low/high sensitivity non-gating，模型选择不得读取任何路由方法效果；
- 数据与运行：`CAL_PHYS_SOURCE_REGISTRY.csv`、`CAL_PHYS_RAW_DATA_MANIFEST.json`、`CAL_PHYS_RUN_SCHEDULE.csv`；
- 执行门控：`CAL_PHYS_FREEZE_CHECKLIST.md`；当前车型、商品、设备精度、因素水平、独立 n、验收阈值和 freeze identity 未补齐，因此不能将协议标为 frozen；
- 工程前置：`docs/当前规划/决策与审计/O0CC源码字节基线与行尾处置.md`。提交 D `3f2b7e3…` 已将权威身份封为 compute `9185ad4b…`、control `5332b67e…`、analysis `c1e78f36…`；当前/fresh worktree archive verify、94/94 回归及服务器离机 bundle 三重验证均通过。源码基线已 `sealed`，下一步由独立文档提交 E 纳入 Git，并为 E 建立新备份，不覆盖 D bundle。

### 数据角色（阶段五，已冻结）

| 数据 | seed | 每 cell 实例 | 用途 |
|---|---|---|---|
| DEV-CAL | 777 | 128 | 开发调参与 scale 估计（参与过 slack 调参，非独立校准证据） |
| DEV-PROTO | 7781 | 32 | 协议调试 / parity / service smoke |
| DEV-GATE | 7782 | 128 | 一次性 lookahead 判定（查看结果后不得再改方法） |
| VAL128 | — | 128 | 方法与口径完全冻结后的正式 Gate |
| TEST | — | — | 最后一次性使用 |

三者均用 `generate_coldchain_data.py`（R1/C1/RC1 × EDoD 0.2/0.5/0.8，默认 temp_dist，capacity 50），基线 hard Gate 均 100%（DEV-CAL 1152、DEV-PROTO 288、DEV-GATE 1152 全 PASS）。

### lookahead 进入判据（阶段六，已冻结）

所有分支都先要求 JF1-H-F baseline 与 oracle 100% hard-feasible、协议字段完整、accounting/ownership 一致，以及 sequential 逐实例非退化断言通过。`PROTOCOL_ERROR` 或 `SERVICE_GATE_FAIL` 优先阻断成本解释。

DEV-GATE verdict 的后续路线冻结为：

- **Sequential GO**：当前单客户动作空间取得正式 trajectory headroom；不做 lookahead，冻结报告与动作合同。**DEV-GATE GO ≠ O0-CC 正式关闭**：下一步是 CAL-PHYS → 新 DEV-CAL/profile → 新 DEV-GATE → 正式 O0-CC VAL；只有 VAL GO 后才进入 M0。不把 DEV oracle GO 写成部署性能结论，也不在正式 VAL 前把 M0 teacher 构造当成正式阶段。
- **EVIDENCE_INSUFFICIENT**：先按预声明口径报告总体效应量、单 cell 异质性与 local 可估计性，不在同一 DEV-GATE 上调参。若 local 的九 cell 统计可靠且 GO，则在独立开发数据上进入 depth-2 lookahead；否则进入动作空间诊断。
- **NO_HEADROOM**：若 local GO，说明单动作局部潜力存在但 sequential composition 失败，进入 depth-2 lookahead；若 local 也无 headroom，才检查 two-customer relocate/exchange 或 batch mask action。
- **SERVICE_GATE_FAIL / PROTOCOL_ERROR**：保存现场，不解释 D/Q/E/J；修复若影响 baseline、objective profile 或计算身份，必须重新审计、重新冻结并从空目录重跑。

只有“local 可靠且 GO、sequential 未通过、协议与服务 Gate 全通过”时，才允许进入 1–2 step lookahead。不得依据某一个 cell 显著或中途结果触发该路线。

### 阶段 C 已完成（oracle protocol repair 主体）

- [x] **continuation 隔离**：`Replanner.export_state/restore_state`（JF1-H-F 导出 deferred 集合），snapshot 捕获 `replanner_state` 并在 `run_resumed` 恢复；`make_continuation()` 工厂 + runner 每条 rollout 分支独立 JF1-H-F；
- [x] **决策点上下文对齐**：`StrictOnlineEnv.prepare_decision_point` 抽出，`_incumbent_plans` 复用（no-op == baseline）；`_snapshot_with_force` 只写 mutable 车（非 mutable 车逐字保留，no-op 不得把 WAIT 改返仓）；
- [x] **scope 贯通**：`decision_pool` 排除冻结 tail；`mutable_vehicle_ids` 贯穿 enumerate / NEW_ROUTE physical matching / apply_action / plan hash / force 写回；
- [x] **共享 hard gate**：`hard_gate.py`（`hard_vector_from_outcome` / `hard_pass` / `service_ok`），候选评估用 objective-aware service_ok；`lex_key` 语义保留给 enumerate_counterfactuals；
- [x] **KEEP/DEFER + τ 容差**：sequential 先显式评估「保持当前计划」，候选须 service_ok 且 `J < J_keep − τ`（τ=1e-9+1e-9·max(1,|J_keep|)），未改善则 KEEP/DEFER；
- [x] **local 重写**：独立 baseline-state oracle（每 snapshot 一次 incumbent + 每 customer 独立单动作 rollout + keep==baseline 断言），不再调完整 sequential_oracle_plan；
- [x] **instance-level 统计**：local 三级平均（customer→event→instance）+ instance bootstrap（10000 reps, seed 42, 95%），无 eligible event 实例单独计数不静默删除；
- [x] **严格 v2 加载**：coldchain 必须显式 `--objective-profile`，禁止静默回退 pilot；reload hash 校验；拒绝 v1（INVALIDATED）；worker 收完整 manifest 并复算 hash；
- [x] **定向测试**：`test_oracle_isolation.py`（baseline 确定性 / 候选重复评估一致 / 顺序无关，3/3）+ 回归 `test_sequential_oracle` 6/6、`test_action_contract` 8/8、`test_counterfactual_teacher` 4/4、`test_jf1h_repair` 3/3；
### 阶段 C 复核补丁（第三轮：协议完整性）

- [x] **PROTOCOL_ERROR 补全**：`outcome_protocol_error(outcome, objective)` 覆盖缺必需字段/类型错误/NaN/Inf/**accounting 不一致**（trace/distance accounting=false 判协议错误，非 service fail）；`n_unserved/n_duplicate` 缺失不再默认 0；`select_improving` 防御性拒绝 NaN/Inf；local 候选路径补上 `outcome_protocol_error`；
- [x] **snapshot 完整性**：`restore_recourse_snapshot` 前核验 `state_hash`（篡改 clock/suffix/deferred → 报错）；`export_state` 加 version/baseline 标识，`restore_state` 校验版本，缺 state 不再静默沿用；
- [x] **基线一致性脚本补强**：`verify_baseline_consistency.py` 比较完整 hard vector（含 repair-level）+ D/Q/E 有限值检查 + 逐分量最大误差 + 非零退出码 + `baseline_consistency.json`；
- [x] **sequential 终局非退化**：runner 逐实例 `J_oracle <= J_baseline + 固定浮点容差`，超出 → `PROTOCOL_ERROR`；`--role`（regression/dev_proto/dev_gate/val）门控 verdict，regression 不产 GO；`--instance-ids` 精确选样，禁止静默缩小样本；
- [x] **coverage 正名**：`n_divergences` → `n_accepted_actions` + `first_plan_change_seq`；
- [x] **数据 manifest 版本化**：`generate_dev_split.py` 加 `split_role` + `scene_instance_id` + 内部 instance seed；`check_hard_gate.py` FAIL 时非零退出 + 逐实例结果；
- [x] **测试**：`test_protocol_integrity.py`（snapshot hash 篡改 / restore 版本 / synthetic）3/3；`test_deferred_rollout_isolation.py`（A→B→A coldchain rollout）1/1；`test_oracle_selection.py` 补 accounting/NaN/-Inf/缺 n_unserved 反例 4/4；全量 10 测试文件 PASS。

- [x] round-4/5 reg8 + workers parity + 跨场景 9 cell 回归均完成（见后续「阶段 C 最终关闭」「启动前收尾」节）。

### 第四轮复核（协议完整性）已完成

- [x] **verify2 通过（Gate）**：修正版 `verify_baseline_consistency.py`（从 `scale_statistics.json` 读 cells，前置校验 9 文件 hash/配置/1152 实例），**1152/1152 全部 MATCH**（D/Q/E max error=0 + 完整 hard vector 一致）→ v2 scale 继续有效；
- [x] **round-3 proto 归档为诊断**：42:51 时 1/8 实例，无 repair 审计（旧代码未采集），版本溯源不完整，已写 `ROUND3_DIAGNOSTIC.txt`，不据此做正式判断；
- [x] **round-4 代码冻结**：6 个核心文件 hash 已记录（run_action_oracle / hard_gate / sequential_oracle / jf1h_repair / recourse_snapshot / action_contract）；
- [x] 终局协议校验 / repair 审计 / 精确选样 / snapshot 完整性 / 真断言测试 全部落地（第四轮）；
- [x] round-4 2 实例 workers parity 通过（w1==w2，mismatch 0）。

### 阶段 C 最终关闭（第五轮：候选 repair 审计 + 跨场景回归）

- [x] **候选 repair 审计贯通**：`_eval` 从 `env.replanner` 附加 `ownership_violations`/`terminal_unresolved`（覆盖 baseline/KEEP/候选/local/sequential）；`outcome_protocol_error` 加 ownership>0→PROTOCOL_ERROR；`service_ok` 加 terminal>0→service fail；小数计数→协议错误；
- [x] **verify 配置断言**：`num_vehicles` 与冻结 provenance 比较（25 一致）、`checked==1152` 显式要求；
- [x] **测试**：ownership/terminal/小数 注入反例 + A→B→A 补 hard vector/决策状态/分支审计；
- [x] **r5 重跑**：parity w1==w2（mismatch 0）；reg8 逐实例 ownership=(0,0)，**r5_delta==r4_delta 完全一致**（候选审计不改变结果）→ PROTOCOL_PASS；
- [x] **跨场景小回归**（9 cell × 2 实例，coldchain v2 sequential+local）：**9/9 PROTOCOL_PASS**，ownership=0/terminal=0，每 cell oracle 均改善 coldchain cost（mean_delta 全负，CI 不跨 0）；
- [x] **阶段 C 关闭**。

### DEV-GATE 分析清单（已冻结，决策 3）

- 口径：**每 cell 128，总计 1152**（R1/C1/RC1 × EDoD 0.2/0.5/0.8）；DEV-GATE seed=7782（已生成 + manifest）。
- 主终点：九 cell 等权的 sequential 平均配对 Δ（paired bootstrap CI）；local 为辅助路线判据（instance-level 聚合 + 无 eligible 报告）。
- 统计依赖：九 cell 共用同一组内部 instance seeds → **按内部 seed 成组 bootstrap**（每次抽 128 个 seed 组，保留每组九 cell 结果再算等权均值），不得把 1152 条当独立样本。
- 单 cell 结果全部报告，但不采用「任一 cell 显著即整体 GO」；任一协议错误或服务 Gate 失败先阻断正式成本判定。
- 暂不做性能优化、不加 lookahead、不改变动作空间。

### 阶段 C 启动前收尾（第六轮：防错 + 汇总入口，已冻结）

- [x] **local baseline served_mask**：`_local_instance` 的 baseline `_eval` 传 `served_mask` + `outcome_protocol_error(strict_repair=True)` + `service_ok`；
- [x] **严格 repair 审计模式**：`outcome_protocol_error(outcome, objective, strict_repair)` —— strict 下 `ownership_violations`/`terminal_unresolved` 必须存在、为有限非负整数（不 int() 截断），缺失报协议错误；`sequential_oracle_plan` 用 `hasattr(continuation,'repair_stats')` 条件化 strict（JF1-H-F 强制、旧 JF1-H 跳过）；
- [x] **聚合器加固**：`aggregate_cross_cell.py` 重写为正式入口——身份按 data hash 映射（倒序输入不变）、拒绝重复/缺失 cell、formal 强制 128 实例、逐实例 Gate 复算（protocol/service/repair/非退化/有限，不信旧 summary）、九 cell seed 一致校验、`regression`/`dev_gate` 角色门控、local 按 seed 成组重采样；
- [x] **聚合器测试** `test_aggregate_cross_cell.py`：order 不变 / duplicate 拒绝 / missing 拒绝 / NaN 拒绝 / service-fail 拒绝，**5/5 PASS**；
- [x] **接线回归**：r6_wire（strict_repair + local served_mask）与 r5_parity_w1 逐实例 D/Q/E/J/动作 **完全一致**（mismatch 0）→ 纯增量审计不改变行为；
- [x] **旧八文件冻结记录（已作废）**：8 个核心文件 hash 记录（hard_gate `aae966a8…`、sequential_oracle `2ddb402e…`、run_action_oracle `38461263…`、aggregate_cross_cell `0a8bd90f…`、counterfactual_teacher `b87b604e…`、jf1h_repair `233f8439…`、recourse_snapshot `0af3535a…`、action_contract `9d64273a…`）。
  - ⚠️ 该 8 文件「最终冻结」后被发现不足：runner 在运行中漂移为 `a7b4c53e…`（manifest 自报 a7b4c53e 但实际执行 38461263，坐实「运行结束重读磁盘」的版本谎报 bug）。已由 15 文件 compute 清单（补入 `project_paths.py`）+ runner 启动时 self-hash + 每 cell/续跑/聚合前重验核取代。旧批次归档 `results/o0cc/dev_gate_drift_diag_v1/`，仅作诊断、非正式判定依据。

**DEV-GATE 预算更新（2026-09-08 复核）**：旧 r5 reg8 平均 966.48s（16.1 分钟/实例）的外推 **39 小时**已作废——实际漂移归档批次的 128 个诊断实例平均 **1831.55s（30.5 分钟/实例）**，8 workers 机械外推 1152 × 1831.55s ÷ 8 ≈ **73 小时**（约三天起步并留余量；单 cell 且受负载影响，非承诺值）。

### 阶段 C 复核补丁（第二轮源码复核）

- [x] **NEW_ROUTE apply 越界 bug**：`apply_action` 原在移除 src 客户后才 `_match_new_route`，导致 src 车变「空 idle」被误选为目标（candidate 按 v2 算、apply 落到 v1，产生 no-op）。改为移除 src **前**在原始 plans 上确定 target；
- [x] **顺序无关选择**：`hard_gate.select_improving(cost_id_pairs, keep_cost, τ)` —— τ 只判断是否离开 KEEP，改善候选间按 (cost, action_id) 稳定取最小；sequential 与 local 共用；
- [x] **restore_state 完整性**：JF1-H-F 恢复 deferred + 重置分支审计状态（repair_stats/events/status），`state=None` 显式报错（legacy 快照不得沿用当前对象）；force 应用后 `sync_deferred_from_vehicles` 同步 ownership；
- [x] **PROTOCOL_ERROR**：`hard_gate.outcome_protocol_error(outcome, objective)` 区分「合法服务失败（候选拒绝）」vs「NaN/Inf/缺字段（PROTOCOL_ERROR）」；KEEP outcome 也强制校验，不用 NaN 当比较基准；
- [x] **公共 teacher 统一**：`enumerate_counterfactuals` 复用 `_incumbent_plans` + mutable scope 贯通（不再自行 restore + 直接 plan）；
- [x] **定向测试**：`test_oracle_selection.py`（select_improving 顺序无关/τ 阈值、protocol error、service_ok）4/4；回归 test_counterfactual_teacher 4/4（scenario 改 t=0 idle 车间移动）；全量 P0-S/A/U + oracle 回归 PASS。
